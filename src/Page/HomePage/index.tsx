import React from 'react';
import GameplayContainer from '../../Container/HomeContainer';

const GameplayPage = () => {
  return <GameplayContainer />; // Using GameplayContainer
};

export default GameplayPage;
